import styled from "styled-components";
import colorPallete from "../../common/styles/colorpalette";

const StyledMenteePanel = styled.section`
    padding-top: 100px;
    overflow-x: hidden;
    position: relative;

    p {
        color: ${colorPallete.basic.white};
    }
`

export default StyledMenteePanel;